# ---------------------------------------------------------------------------
# s2-chips-to-map — pinned dependencies
#
# Target environment: Google Colab (Ubuntu 22.04, Python 3.11/3.12, NVIDIA T4).
# Colab already ships torch/torchvision built against the correct CUDA version,
# so torch is listed with a lower bound rather than an exact pin: forcing an
# exact torch pin on Colab triggers a multi-GB reinstall and often a CUDA
# mismatch. Everything else is pinned exactly.
#
# Install order matters on Colab: install the geospatial stack before samgeo.
# ---------------------------------------------------------------------------

# --- core numerics -----------------------------------------------------------
numpy==1.26.4
scipy==1.13.1
pandas==2.2.2
scikit-learn==1.5.1
matplotlib==3.9.2
seaborn==0.13.2
tqdm==4.66.5
pyyaml==6.0.2

# --- deep learning -----------------------------------------------------------
torch>=2.3,<3
torchvision>=0.18,<1
timm==1.0.9

# --- datasets ----------------------------------------------------------------
# torchgeo provides the EuroSAT / EuroSAT100 loaders with auto-download.
# datasets is the documented fallback path (HuggingFace hub copies of EuroSAT).
torchgeo==0.6.0
datasets==2.21.0
huggingface-hub==0.24.6

# --- vision-language ---------------------------------------------------------
# open_clip is required for RemoteCLIP: the released RemoteCLIP checkpoints are
# OpenCLIP state_dicts, not HuggingFace CLIPModel checkpoints.
open-clip-torch==2.26.1
ftfy==6.2.3
regex==2024.7.24

# --- geospatial (NB05) -------------------------------------------------------
rasterio==1.3.10
pyproj==3.6.1
shapely==2.0.5
pystac-client==0.8.3
geopandas==1.0.1
contextily==1.6.2

# --- dimensionality reduction (NB03 embedding plots) -------------------------
umap-learn==0.5.6

# --- explainability (NB06) ---------------------------------------------------
grad-cam==1.5.4

# --- tooling -----------------------------------------------------------------
pytest==8.3.2
ruff==0.6.4

# --- optional / installed inside NB05 only -----------------------------------
# These are heavy and fragile (groundingdino builds a CUDA extension). They are
# installed from within notebook 05 behind a try/except so a failure degrades
# the notebook gracefully instead of killing the kernel. Listed here for the
# record, deliberately NOT installed by `make install`:
#
#   segment-geospatial==0.12.3
#   segment-anything-py==1.0.1
#   groundingdino-py==0.4.0
